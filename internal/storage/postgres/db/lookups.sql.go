// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.27.0
// source: lookups.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createLookup = `-- name: CreateLookup :one
INSERT INTO lookups (kind, value) VALUES ($1, $2)
ON CONFLICT (kind, value) DO UPDATE SET value = EXCLUDED.value
RETURNING id, kind, value, created_at
`

type CreateLookupParams struct {
	Kind  string `json:"kind"`
	Value string `json:"value"`
}

func (q *Queries) CreateLookup(ctx context.Context, arg CreateLookupParams) (Lookup, error) {
	row := q.db.QueryRow(ctx, createLookup, arg.Kind, arg.Value)
	var i Lookup
	err := row.Scan(
		&i.ID,
		&i.Kind,
		&i.Value,
		&i.CreatedAt,
	)
	return i, err
}

const deleteLookup = `-- name: DeleteLookup :exec
DELETE FROM lookups WHERE id = $1
`

func (q *Queries) DeleteLookup(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteLookup, id)
	return err
}

const listLookups = `-- name: ListLookups :many
SELECT id, kind, value, created_at FROM lookups WHERE kind = $1 ORDER BY value
`

func (q *Queries) ListLookups(ctx context.Context, kind string) ([]Lookup, error) {
	rows, err := q.db.Query(ctx, listLookups, kind)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Lookup{}
	for rows.Next() {
		var i Lookup
		if err := rows.Scan(
			&i.ID,
			&i.Kind,
			&i.Value,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
