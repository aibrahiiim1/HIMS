# Binaries
/bin/
*.exe
*.test
*.out

# Go workspace
go.work
go.work.sum

# Env / secrets — never commit credentials
.env
*.local

# Node / frontend (added when the web app lands)
node_modules/
web/dist/

# Editor / OS
.idea/
.vscode/
.DS_Store
*.swp
*.tmp
bin/dev-encryption-key

# Docker deployment secrets/env (never commit)
deploy/docker/.env
deploy/docker/*.secret
# Linux filled-in env (only the .example is tracked)
deploy/linux/hims-api.env
