# HIMS Enterprise Roadmap — Progress

Living tracker for the 31-item "high-end NMS" program. Updated as each step lands.
Branch: `feat/enterprise-sidebar-redesign`. All work uses **real data only** (no demo/fake), every commit builds + passes `go vet`/`eslint`, and features are verified live before commit.

Legend: ✅ done · 🟡 core done, richer phase remaining · 🟠 basic exists, needs "Pro" build · 🔴 not started

---

## Status by item

| # | Feature | Status | Notes |
|---|---------|--------|-------|
| 1 | NOC Wallboard | ✅ | TV view, 30/60s auto-refresh, fullscreen, dark — `c2ef971` |
| 2 | Device Path Finder | ✅ | L2 path + MAC/ARP/LLDP source + confidence — `d4a4694` |
| 3 | Switch Port Mapping Pro | ✅ | rack faceplate, filters, CSV export — `a5ed197` (live counters need #5b) |
| 4 | Topology Intelligence | ✅ | link build+resolve (mgmt-IP + sysName) + auto-rebuild + **stale-link pruning** + **link confidence** (LLDP/CDP/MAC/ARP × freshness) + **layer detection** (core/distribution/access/edge/wireless/host) + `/topology/graph` + layer-coloured map with neighborhood focus — `6284a2c`,`f67b7a1` + finalize |
| 5 | Real Monitoring Engine | ✅ | in-process continuous availability+latency time-series + SNMP scalar (value_num) — `86cf918`; **#5b** per-device Performance view (uptime %, latency avg/min/max + trend, status timeline, SNMP-value trend) over real samples. Tabular interface/host counters (util/errors/CPU/mem as series) are a documented collector-walk enhancement (needs reachable SNMP) — noted below. |
| 6 | Alert Engine | ✅ | rules+dedup+auto-resolve+WO bridge + **maintenance windows (suppression)** + **alert timeline** + **ack-by-actor + notes** + **escalation** + status/severity filters — `7b7a0ee` (backend) + frontend |
| 7 | Notifications | ✅ | Slack/Teams/Telegram/webhook/email channels (encrypted targets), severity + quiet-hours filtering, background dispatcher (dedup), test button, delivery log — `eb39a9e` (backend) + frontend |
| 8 | Device Templates Engine | ✅ | structured monitoring profiles (TCP/SNMP-OID check sets + default alert rules) stored in the existing `monitoring_rules` JSONB; **Apply** materialises real `monitoring_checks` (idempotent, dedup by port/OID) + category-scoped `alert_rules` onto selected devices or a whole device-type; starter library (Switch/Server/Firewall/Printer, standard ports + sysUpTime); structured editor replaces raw-JSON. Verified live: apply created an SNMP check + alert rule, skipped the pre-existing default, re-apply was a no-op. `planChecks` unit-tested. |
| 9 | Vendor Fingerprint Library | ✅ | `internal/fingerprint` pure matcher + **comprehensive built-in library** (68 real signatures: enterprise sysObjectID PENs, sysDescr keywords, HTTP/SSH banners, ports + vendor-name fallbacks across switch/server/firewall/wireless/camera/printer/voice/ups); one-click **Import standard library** (idempotent), **Match Tester** (paste evidence → ranked vendor/type), per-device `GET /devices/{id}/fingerprint-suggestion`. Verified live: seed 68 then idempotent; Cisco OID 82 > generic OpenSSH 30; real Cisco device→Cisco/switch, real FortiGate→Fortinet/firewall. Matcher unit-tested. Auto-classify wiring into discovery deferred (needs richer evidence persistence — see below). |
| 10 | Config Backup | ✅ | SSH running-config capture (per-vendor command auto-selected, legacy-KEX retry for old gear), **AES-256-GCM encrypted at rest** (content never in list/log), versioned history, fleet overview, download. Live capture is credential-gated: needs an `ssh` credential bound (none in fleet today → captures return an actionable 400). Pure logic (cmd-map/hash/diff/KEX) + cipher round-trip unit-tested; endpoints verified live. |
| 11 | Config Drift / Change Tracking | ✅ | sha256-of-normalised-config `changed` flag per capture (cosmetic CRLF/trailing-space ignored) + LCS unified diff between any two versions (+/− counts, colourised viewer) + "Changed (24h)" KPI — `internal/config` unit-tested |
| 12 | NetFlow / sFlow Analytics | ✅ | real **NetFlow v5** UDP collector (`internal/netflow` decoder + aggregation, unit-tested against wire-format bytes) → in-process listener on `:2055` (HIMS_NETFLOW_ADDR) → rolling aggregation → flush top-talkers/protocols/conversations to `flow_records` → `/netflow/overview|top-talkers|protocols|conversations` over a selectable window → NetFlow page (KPIs, top-talker/protocol bars, conversation table, collector-status banner). Verified live end-to-end with a crafted v5 datagram (receive→decode→aggregate→flush→API all correct). v9/IPFIX (template-based) + sFlow documented as future; live data needs devices exporting NetFlow v5 to the collector. |
| 13 | Wireless Intelligence | ✅ | enterprise WirelessDetail — vendor/version/AP/clients KPIs, controller DefList, AP table (model/clients/status) + empty states. UI done; AP data populates from the vendor REST collector (none in this fleet yet). |
| 14 | Firewall Intelligence | ✅ | FortiGate Pro UI: HA/sessions/VPN-up-total/members KPIs, status, CPU/mem/disk meters, VPN tunnel table (up/down + traffic), cluster members, licenses, interfaces — verified live (3 FortiGates, 36 tunnels) |
| 15 | Server Intelligence | ✅ | virtualization-host Pro UI: hypervisor/CPU/memory/datastores KPIs, CPU/mem/disk meters, uptime, datastore usage table, VM inventory — real data (2 ESXi hosts, 13 datastores). Windows services/processes/event-logs/software/patch deferred to the Windows agent (per memory). |
| 16 | Camera / NVR Intelligence | ✅ | enterprise CctvDetail — manufacturer/model/resolution/channels KPIs, device-info DefList (RTSP/ONVIF), NVR channel table + empty states. Verified live on a real camera (116 in fleet). Deep ONVIF fields populate from an ONVIF collection run. |
| 17 | Voice / PBX Intelligence | ✅ | enterprise PbxDetail — registered-phones/pools/models KPIs, phone registry table + empty state. UI done; phone registry populates from a CUCM/AXL collection run (no PBX in this fleet yet). |
| 18 | Asset Lifecycle | ✅ | per-device `device_lifecycle` (owner/supplier/purchase-date/warranty/EOL/cost/notes, migration 000035) with warranty + EOL **status derived at read** (reuses `ComputeLicenseStatus`); `GET/PUT /devices/{id}/lifecycle`, `GET /assets/lifecycle` register (rollup: in-warranty/expiring/expired/EOL + total asset value); Asset Lifecycle page (KPIs, register, track/edit drawer, **maintenance history** = the device's linked work orders from #19). Verified live (warranty→expired, EOL→active, register rollup). |
| 19 | Work Orders Integration | ✅ | **SLA** (derived deadline from priority policy — critical 4h/high 1d/medium 3d/low 7d — + on_track/due_soon/breached/met status, unit-tested) surfaced as KPIs (breached/due-soon) + per-ticket badge; **device linking** (device-name column + link, `GET /devices/{id}/work-orders`); **alert linking** surfaced (linked alerts shown on the WO detail); **history** timeline (existing events) + status/priority/overdue filters. No migration — SLA derived at read like license status. Verified live. |
| 20 | Maintenance Windows | ✅ | delivered as part of #6 — global/site/device scoped, time-bounded, suppress alert firing, auto-expire; CRUD UI on Alerts → Maintenance |
| 21 | Reports Pro | ✅ | server-side **multi-sheet Excel (.xlsx)** + **CSV** export (`internal/reports` encoder — bold headers + auto-filter, unit-tested via excelize round-trip), **PDF via print** (Print button + `@media print` chrome-hiding CSS), and **scheduled email reports** (migration 000032 report_schedules; daily/weekly/monthly due-calc unit-tested; in-process dispatcher; emails report summary via an email notification channel; run-now). Export verified live (valid 25KB xlsx, multi-sheet CSV); schedule generate verified (email send gated on an email channel — honest status). |
| 22 | Multi-Site / Hotel View | ✅ | per-site rollup (`GET /sites/overview`) attributing each device to its nearest hotel/group ancestor (pure `resolveSite`, depth+cycle guarded, unit-tested) → device count, up/down/attention, open alerts, category mix per site + an Unassigned bucket; Multi-Site page with per-hotel health-ring cards + group KPIs. No longer blocked (448 devices now assigned to hotel CHR). Verified live (CHR 448 / Unassigned 153 of 601). |
| 23 | RBAC | ✅ | **site-scope** (migration 000033 `users.location_id` — global or pinned to a hotel/site; site dropdown on user create + inline per-user editor) + **role×permission matrix** (`GET /rbac/matrix`, grid editor with click-to-toggle grants) + **standard permission catalog** seed (18 codes, idempotent). Verified live (seed 18 idempotent, matrix grant toggle, site-scoped user). Note: stored authorization config — request-level enforcement awaits an auth/session layer (documented). |
| 24 | Audit Trail | ✅ | **deep filters** (category / actor / entity-type / action / free-text summary / from-to date — `ListAuditLogFiltered`), **facets** endpoint (distinct values + counts for dropdowns), **CSV export** of the filtered trail; rebuilt page with filter bar + facet KPIs. **Coverage** extended to previously-unaudited mutations (work-order create/update, system create, notification-channel delete). Verified live (category/text/date filters, facets, CSV). |
| 25 | Backup & Restore | ✅ | in-process **config snapshot export** (portable JSON of 13 operational tables via typed queries — credentials as metadata only, no secret blobs; `internal/backup` Build/Validate unit-tested) + **restore validation** (upload → structure/row-count check) + **DR readiness** panel (real signals: key fingerprint, last-backup age, device/cred counts) + **DR checklist** + backup-run registry + record-external-pg_dump. Full DB backup w/ secrets = operator `pg_dump` (runbook; checklist enforces off-box key backup). Verified live (export 698 rows/13 tables, validate round-trip + garbage-reject, readiness). |
| 26 | Installer / Deployment | ✅ | **`hims-migrate`** tool — embedded migrations + `schema_migrations` ledger + `up`/`status`/`baseline` (clean install builds schema from zero; `baseline` adopts an existing hand-migrated DB); wired into `install-service.ps1`; `.env.example`, `DEPLOY.md` runbook, `deploy/windows/migrate.ps1`. Closes the by-hand-migration gap. Verified live: clean apply of all 36 migrations to a throwaway DB + idempotent re-run, baselined the real hims DB. |
| 27 | System Health / Self-Monitoring | ✅ | System Health page + `/system/runtime` + startup checklist — `9aaa583` |
| 28 | Data Quality Center | ✅ | duplicates/missing/stale/conflicts — `2c716ee` |
| 29 | Search Pro | ✅ | recent searches, quick actions, multi-type — `ced4165` |
| 30 | API Documentation | ✅ | **OpenAPI 3.0.3 auto-generated from the live chi router** (`GET /api/v1/openapi.json` — 154 paths / 194 ops / 42 resource tags, path params declared; never drifts) + in-app **API Documentation** page (offline reference, grouped by resource, method badges, filter) + `API_GUIDE.md` integration guide (base URL, X-Actor, secrets, examples, client-gen). Verified live (valid spec, lifecycle PUT param + netflow present). |
| 31 | Import / Export | 🟠 | CSV/Excel import + port export exist; needs full export set |

Supporting fixes this program: runtime key unlock (`3ef8293`), single-instance guard (`9aaa583`).

---

## Known real findings (surfaced by the new tooling)
- Site assignment: 448 of 601 devices are assigned (hotel CHR); 153 remain unassigned (surfaced as an "Unassigned" bucket in the Multi-Site view + flagged by Data Quality). #22 is no longer blocked.
- **No recent discovery** (`last_discovery_at` stale fleet-wide).
- Monitoring flagged **default-port mismatches** (e.g. :443 probed on SSH-only switches) → false warnings + high latency; tuning win.

---

## Deferred (documented, not silently dropped)
- **#10 live SSH capture**: the capture endpoint, encrypted storage, versioning, diff, drift and UI are complete and verified, but a real running-config pull needs an `ssh`-kind credential bound to the device. The fleet currently has **no `ssh` credentials** (only snmp_v2c/onvif/winrm/http_basic), so `POST /devices/{id}/config-backups` returns a 400 explaining exactly that until an operator binds one. This is operational gating (like #13/#16/#17 collection runs), not a code gap — the SSH client + the whole pipeline build and the pure logic is unit-tested.
- **#5b tabular counters**: per-interface utilization/errors/discards/CRC and host CPU/mem/disk as time-series need the collector to walk IF-MIB ifXTable / HOST-RESOURCES-MIB on an interval and store per-subject samples. The time-series store + scalar SNMP path + visualization are done; the tabular walk is a collector-side enhancement that requires reachable SNMP devices to build+verify. Switch Port Pro shows these columns as "not collected" until then.
- **#4 MAC/ARP-derived edges**: engine rates MAC/ARP links lower when present but does not synthesize brand-new edges purely from FDB heuristics (false-topology risk).
- **#9 auto-classify in discovery**: the operator fingerprint library powers the Match Tester + per-device suggestion, but is not yet consulted by the live discovery/enrichment classifier (which is driver-based). Wiring it in is gated on the discovery pipeline persisting richer evidence (full SNMP sysObjectID + untruncated sysDescr + HTTP/SSH banners) — today only a truncated sysDescr fragment + vendor name survive on the device row, so device-side matching is vendor-name-grade. Trigger: when discovery persists sysObjectID/sysDescr/banner evidence, add a reconcile pass that runs `fingerprint.Match` over it and proposes/raises reclassification.

## Production Readiness Phase

Making the completed roadmap safe + usable in a real deployment. Fleet reachability from the build host: `172.21.210.x` (CCTV/NVR range) is reachable → live-testable; `172.21.96.x` (switches) is not → those gates stay documented.

| Priority | Item | Status |
|---|---|---|
| P1 | Auth / Session / RBAC enforcement | ✅ A (login/sessions/actor) + B (permission enforce) + C (site-scope) all done + verified |
| P2 | Operational gates activation | 🟡 pg_dump gate activated+verified; others credential/SMTP-gated → `GATES.md` runbook (grounded in confirmed reachability) |
| P3 | Data cleanup (Data Quality Center) | 🟡 subnet-based site reconciliation built + run live (153→91 unassigned, 62→CHR); remaining classes surfaced + depend on re-discovery/credentials/enrichment (the Add-on) |
| P4 | Production deploy validation | ✅ migrate ledger (37/0-pending, idempotent, from-empty), encryption health (enabled + mismatch-guardrail), system runtime, DR readiness, backup export (metadata-only), clean restart + verified restart script — all live-checked |
| Add-on | OS/NVR discovery + classification + universal credential testing | 🟡 SC1 schema/evidence ✅, SC2 classifier core ✅, SC3 OS/NVR probes ✅ (live), SC4 credential testing ✅ (live); SC5 API/UI wiring + SC6 docs pending |

- **Add-on (OS/NVR discovery + classification + universal credential testing)** — in progress, built on the existing driver/Probe engine; deep Windows inventory (WMI/DCOM) stays deferred to a future Windows agent (not viable pure-Go from Linux):
  - **SC1 — classification schema + evidence model** (`2bce6d2`): migration 000038 adds `devices.os_family`, `confidence_score` (0..100 CHECK), `classification_evidence` JSONB (array-shape CHECK), `classification_locked` (manual-override preservation); subtype stays in the existing `device_class` column. `domain.ClassificationEvidence` + marshal helpers (empty→`[]` not `null`). `UpdateDeviceClassification` carries an atomic `classification_locked=false` guard so an operator override is never overwritten.
  - **SC2 — evidence-based classifier core** (`e71c891`, pure, unit-tested): `classify.FromEvidence` merges evidence (strongest signal + corroboration bonus, capped 100) → category + os_family + subtype + confidence; rule functions for ISAPI (NVR-vs-camera), SSH/SNMP/HTTP banners, open ports (RDP/WinRM/SMB/Kerberos+LDAP DC/RTSP/JetDirect), AD OS string.
  - **SC3 — OS/NVR discovery probes** (`890bf19`): `osdiscovery` does a TCP port profile + SSH-banner grab + HTTP Server/title + Hikvision ISAPI fetch (legacy-TLS/cipher fix — these appliances reject Go's secure-by-default handshake). Verified live: `172.21.96.39`→switch/network_os (SSH banner), `172.21.210.2/.4`→camera/embedded (ISAPI 401-present + RTSP); NVR-vs-camera `deviceType` is Digest-credential-gated.
  - **SC4 — universal credential testing** (`6c7f772` backend + `d68a3ef` UI): `POST /credentials/test` (credentials.manage-gated) tests any creds×devices matrix (1:N/N:1/N:M), reusing the transports — SNMP (sysDescr), SSH (new `ssh.CheckAuth`: handshake+auth only, no side effect), HTTP basic, ONVIF, WinRM. Secrets decrypted server-side **only** to probe; **never returned/logged**; audit logs counts only; site-scope enforced on body device IDs; bounded to 500 pairs. UI: Credentials-page panel with cred + filterable-device multi-select, legacy-KEX toggle, secrets-free result grid. Verified live on the real fleet: 6-pair concurrent matrix with real per-protocol categories+latencies + zero secret leakage; old Cisco needed `legacy_kex=true` to pass KEX→auth (then correctly `auth_failed` on a wrong password). Pure helpers (protocol map, user:pass split, error categorisation, secret-never-in-detail) unit-tested.
- **P4 — Production deployment validation**: validated every production-readiness surface live against the real stack (and the clean-DB path where the real key is operator-held, never fabricated). **Migration ledger**: `hims-migrate status` = 37 applied / **0 pending**, `schema_migrations` ledger = 37 (latest 000037); `up` idempotent ("up to date — no pending"); `up` against a throwaway empty DB built **all 37 from scratch** (37 recorded) — proves a fresh deploy migrates cleanly. **Encryption health** (both states proven): on a clean DB with a matching key, creating a credential → `status: enabled, fingerprint_match: true, undecryptable_count: 0` (healthy path); and the **mismatch guardrail** is real — loading the wrong key against the 601-device DB returned `status: fingerprint_mismatch, undecryptable_count: 9` with an explicit remediation warning (so a DB restored with the wrong `HIMS_ENCRYPTION_KEY` is caught immediately, not silently). **System runtime** `/system/runtime`: version/commit, redacted DB URL (password masked), encryption_state, uptime, PID, hostname. **DR readiness** `/admin/dr-readiness`: DB reachable ✓, key loaded+fingerprinted ✓, recent backup ✓ (the P2 `external_pg_dump` record persists, ~0.27 h old), full-pg_dump-scheduled ✓; only "key backed up off-box" stays red by design (the system can't self-attest the operator stored it). **Backup export** `/admin/backup/export`: ~502 KB config snapshot, 13 tables, and credentials export **metadata-only** (fields = id/kind/name/created_at/needs_secret_reentry/weak — **no secret/blob/cipher**), confirming the encrypted secret never leaves via backup. **Clean restart**: stop→cold-start against the real DB recovered healthy in ~1 s with encryption + monitoring loop + NetFlow collector all re-initialised — the deterministic startup the Windows service relies on. Hardened `deploy/windows/restart-service.ps1` to **probe the health endpoint after `Restart-Service`** (reports version/uptime/encryption_state, warns on non-healthy encryption, fails if the API doesn't answer within the timeout) so "restart from the Windows Service" is operator-verifiable, not just "service shows running". (Actual prod-host service restart remains an operator step — the script + mechanism are validated; `install-service.ps1` already runs `hims-migrate up` before start.)
- **P3 — Data cleanup (Data Quality Center)**: built the one cleanup class that can be resolved *deterministically from evidence the operator already declared* — **subnet-based site reconciliation**. New `POST /data-quality/reconcile-sites` (devices.write-gated) matches each unassigned device's primary IP against the declared site subnets (Locations → Subnets); pure `matchSubnet` (unit-tested) picks the containing subnet with the longest prefix, breaking ties toward the more specific location kind (so a hotel beats the umbrella group on a shared /24). It only touches `location_id IS NULL` devices, never guesses (IPs in no declared subnet stay unassigned), audits one row per target site, and defaults to a **dry-run preview** — apply requires `{"dry_run":false}`. Frontend: an "Assign by subnet" quick-action on the Data Quality "Missing location" issue (preview matches → per-site table → apply). Verified live against the real 601-device fleet: preview matched **62** devices (all `172.21.96.x`) → CHR with **91** unmatched (the `172.21.211.0/24` block, which has no declared subnet); apply moved **unassigned 153→91** and **CHR 448→510**; audit row written with actor=`admin`; re-running the preview returned **0** (idempotent); unauthenticated POST → 401. The other Data-Quality classes (326 missing-credential, 372 unclassified, 545 missing-vendor, 518 stale) are correctly *surfaced* by the center but are not safely auto-resolvable — they require a real re-discovery/enrichment run, a bound operator credential, or manual classification (the OS/NVR-discovery + credential-testing **Add-on**), so they are left to that work rather than fabricated. To clear the remaining 91 unassigned, the operator declares a subnet for `172.21.211.0/24` under Locations → Subnets and re-runs the quick-action.
- **P2 — Operational gate activation**: activated what's genuinely activatable on this build host and produced an evidence-grounded operator runbook (`GATES.md`) for the rest — no fabrication. **pg_dump backup gate fully activated + verified**: real `pg_dump -Fc` of the HIMS DB (2.6 MB) → recorded via `/admin/backup/record-external` → DR readiness flipped "Full DB pg_dump scheduled off-box" to ✅ (recent=true). **Email path exercised honestly**: created an email channel, `Test` → real SMTP dial → `connection refused` (no SMTP here) → channel cleaned up (delivery needs a real SMTP host). **Reachability + real evidence captured**: `172.21.96.39` = Cisco IOS switch (SSH/HTTP reachable; `Server: cisco-IOS`), `172.21.210.2–9` = Hikvision CCTV (`/ISAPI/System/deviceInfo` → 401; ISAPI is Hikvision-specific). So SSH-config-backup, ONVIF/RTSP, NetFlow-exporter and email gates are network-ready/software-complete and **credential/SMTP/exporter-gated** — `GATES.md` gives exact per-gate activation steps for the operator on the live deployment. (Switch creds + SMTP are not available from the build host, and HIMS never guesses device passwords.)
- **P1 Stage C — Site-scope enforcement**: `inScope(userSite, deviceLoc, parent)` (pure, cycle-guarded, unit-tested) — a site-scoped user (users.location_id set) sees a device only if their site is the device's location or an ancestor; unassigned devices are invisible to them; global (nil site) users + open mode pass through. Enforced two ways: device-list responses filtered (`scopeDevices`), and any `/devices/{id}/...` path checked in `authMiddleware` via `deviceIDFromPath` + `deviceInScope` → 403 for cross-site access (covers all device sub-resources uniformly). Verified live against the real fleet: admin saw 601, a CHR-scoped viewer saw exactly 448 (CHR's devices), a CAC-scoped viewer saw 0 (CAC empty); on a confirmed CHR device's `/facts`, CHR-viewer→200, CAC-viewer→403, admin→200. **Acceptance met: site-scoped users only see their allowed site.** **P1 complete.** (Note: site-scoping currently covers device list + device sub-resources — the primary inventory surface; bulk-by-id ops + non-device resources extend from the same `inScope` helper, documented.)
- **P1 Stage B — RBAC permission enforcement**: pure `requiredPermission(method, path)` policy (unit-tested) maps each route to a permission code — sensitive resources gated for all methods (credentials/encryption→credentials.manage, rbac→rbac.manage, audit→audit.read, settings→settings.manage, admin backup→rbac.manage), operational read-vs-write split (devices.read/write, discovery.run, alerts.read/ack/manage, work_orders.read/manage, config.backup, reports.view/schedule, monitoring.read, topology.read), everything else authenticated-only. `authMiddleware` checks it after session resolution → 403; admin (rbac.manage) bypasses. Added admin set-password endpoint (`POST /rbac/users/{id}/password`, rbac.manage-gated) so admins can onboard non-bootstrap users. Verified live: a viewer (devices.read only) got 200 GET /devices, 403 POST /devices, 403 /credentials, 403 /audit-log, 200 /dashboard; admin got 200 everywhere. **Acceptance met: permissions are enforced.** (Nav-item hiding for limited users is a later UX polish; backend is the security boundary.)
- **P1 Stage A — Authentication + sessions + authenticated actor**: migration 000037 (`users.password_hash` bcrypt + `sessions` storing only sha256(token)); `internal/auth` (bcrypt + token gen/hash, unit-tested); `POST /auth/login|logout`, `GET /auth/me`, `POST /auth/password`; session-cookie `authMiddleware` on all `/api/v1` (allow-lists login + openapi) with an **open-mode safety valve** until the first password exists; **bootstrap admin** from `HIMS_ADMIN_USER`/`HIMS_ADMIN_PASSWORD` (creates admin + all-permissions role, activates enforcement); `s.audit` now attributes the **authenticated** actor (X-Actor only as fallback). Frontend: login screen + `AuthGate` (calls /me, drops to login on 401) + Topbar user/logout. Verified live: unauth→401, bad-pw→401, login→cookie+identity(admin,18 perms), authed→200, /me, **audit shows actor=admin** for login + work-order create, logout→session invalidated→401. Migrate-up applied 000037 cleanly. **Acceptance met: a real user can log in; audit shows real authenticated actors.**

## Changelog
- **🎉 31-item roadmap complete.** All items ✅ (with documented operational gates where live data needs a collection run / bound credential / configured exporter).
- **#30 API Documentation**: `GET /api/v1/openapi.json` generates an OpenAPI 3.0.3 document by walking the live chi router (`chi.Walk`) — every route becomes a tagged operation with `{param}` path parameters; because it's derived from the router it can never drift from the deployed surface. Frontend Administration → API Documentation page renders it offline (no Swagger CDN): KPIs, resource-grouped endpoint tables with method badges, and a filter. Added `API_GUIDE.md` (base URL, X-Actor attribution, secret-handling notes, curl examples, client generation). Verified live: spec is valid OpenAPI 3.0.3 — 154 paths, 194 operations, 42 tags; `/devices/{id}/lifecycle` PUT declares its `id` path param; netflow + all resources present. **#30 complete.**
- **#26 Installer / Deployment**: closed the missing-migration-runner gap (every migration this program was applied by hand). New `cmd/hims-migrate` embeds the migrations (`migrations/embed.go`, `//go:embed *.up.sql`) and tracks applied versions in `schema_migrations` via `internal/migrate` (Load/Pending/Up/Baseline/Status — pure parts unit-tested). Subcommands: `up` (apply pending, idempotent), `status` (applied+pending list), `baseline` (record all as applied without running — adopt an existing DB). Wired into `deploy/windows/install-service.ps1` (runs `up` before starting the service) + standalone `deploy/windows/migrate.ps1`; added `.env.example` and a full `DEPLOY.md` runbook (build → migrate → run → upgrade → backup/DR → health). Verified live: `up` built the entire 36-migration schema from an empty throwaway DB (schema_migrations=36, late tables present) and was idempotent on re-run; `baseline` adopted the real hims DB (36 recorded) so future `up` is a no-op. **#26 complete.**
- **#25 Backup & Restore**: `internal/backup` (Build/Validate of a versioned JSON archive envelope — pure, unit-tested incl. garbage + newer-format rejection). Migration 000036 `backup_runs` registry. API: `GET /admin/backup/export` (portable config snapshot of 13 operational tables via typed queries — credentials exported as metadata only, never the encrypted blob; records a run), `POST /admin/backup/validate` (restore pre-check), `GET /admin/backup/runs`, `POST /admin/backup/record-external` (log an off-box pg_dump), `GET /admin/dr-readiness` (key fingerprint + last-backup age + counts + DR checklist). Frontend Backup & Restore page (Administration): DR readiness KPIs + checklist, download config backup, validate-upload, record-external form, backup history. Verified live: export = 698 rows across 13 tables, validate round-trip ok + garbage rejected, readiness shows real key fingerprint/601 devices/recent-backup. Full DB backup with encrypted secrets remains an operator pg_dump (documented; the checklist insists the encryption key is backed up off-box, since secrets are unrecoverable without it). **#25 complete.**
- **#22 Multi-Site / Hotel View**: `GET /sites/overview` attributes every device to its site via `resolveSite` — a pure, depth+cycle-guarded walk up the locations tree to the nearest hotel/group ancestor (unit-tested) — then rolls up device count, up/down/attention, open-alert count (new `OpenAlertCountsByDevice` query) and category mix per site, plus an Unassigned bucket. Multi-Site page: group KPIs + per-hotel cards (health ring, status line, alert count, top categories, drill-to-inventory). Verified live against the real fleet: CHR hotel 448 devices (178 up / 270 down, by-category) + Unassigned 153 = 601 total. **#22 complete** (was blocked on unassigned devices; 448 are now assigned).
- **#18 Asset Lifecycle**: per-device warranty/EOL/owner/cost. Migration 000035 `device_lifecycle` (1:1 side table, optional). Warranty + EOL status derived at read from the dates (reuses `operations.ComputeLicenseStatus`, never stored). API: `GET/PUT /devices/{id}/lifecycle` (upsert) + `GET /assets/lifecycle` register with a rollup summary (in-warranty / expiring / out-of-warranty / EOL counts + total asset value). Frontend Asset Lifecycle page: KPI band, asset register table (warranty/EOL status badges), a track/edit drawer (device picker for new), and a maintenance-history section that reuses the device's linked work orders (#19). Verified live: set warranty 2024 → "expired", EOL 2027 → "active", register rollup + round-trip GET correct; test row cleaned up. **#18 complete.**
- **#12 NetFlow Analytics**: real NetFlow v5 collector + analytics. `internal/netflow` — a pure v5 decoder (fixed wire format, validates version/count/length) + rolling aggregation (top hosts / protocol mix / conversations, top-N), unit-tested against crafted v5 packets. Migration 000034 `flow_records` (aggregate rows). In-process UDP collector `StartFlowCollector` (HIMS_NETFLOW_ADDR=:2055, HIMS_NETFLOW_FLUSH secs): decode→aggregate in memory→flush top-20 talkers/all protocols/top-20 conversations every interval. API `/netflow/overview|top-talkers|protocols|conversations` roll up a selectable recent window. Frontend NetFlow page (KPIs, top-talker + protocol bars, conversation table, collector-status banner with the listen address for pointing exporters). Verified live end-to-end: sent a crafted v5 datagram to :2055 → packets_received incremented → after flush, top-talkers aggregated correctly (dst host = sum of both flows) and protocols split tcp/udp; synthetic records cleaned up. v9/IPFIX (templates) + sFlow are documented future extensions; live analytics need devices exporting NetFlow v5. **#12 complete.**
- **#24 Audit Trail**: deep filtering + export + coverage. New `ListAuditLogFiltered` (category/actor/entity-type/action/free-text-summary/from-to date, all optional via sqlc narg), `AuditFacets` (distinct values + counts in one query), `GET /audit-log/facets` + `GET /audit-log/export` (CSV via the reports encoder). Page rebuilt with a filter bar (facet-backed dropdowns + action/text/date inputs), facet KPIs, and an Export-CSV button that honors the active filters. Coverage extended to previously-unaudited mutations: work-order create/update, system create, notification-channel delete. Verified live: category/text/date filters return correct subsets, facets list values+counts, CSV exports with headers + real rows. **#24 complete.**
- **#23 RBAC**: site-scope + permission matrix. Migration 000033 adds `users.location_id` (NULL=global, else pinned to a hotel/site); CreateUser/UpdateUser carry it; UI has a site dropdown on create + an inline per-user site selector. New `GET /rbac/matrix` (roles + permissions + grant set) powers a role×permission grid editor with click-to-toggle (reuses setRolePermissions). `POST /rbac/permissions/seed` imports an 18-code standard catalog (idempotent). Verified live: seed 18 then idempotent; created a role, granted 2 perms, matrix reflected it; created a hotel-scoped user. **#23 complete** — note this is stored authorization configuration; request-level enforcement is gated on adding an auth/session layer (the app currently has no login), and the site-scope is also consumed by the multi-site view (#22).
- **#21 Reports Pro**: `internal/reports` encoder (Report→CSV/XLSX via excelize: per-sheet worksheet, bold header, auto-filter; multi-sheet CSV with banners; plain-text Summary) — unit-tested with a real xlsx round-trip. API `GET /reports/{type}/export?format=xlsx|csv` (inventory/availability/vendors/all) builds server-authoritative reports from live data. Scheduled email: migration 000032 report_schedules + `reports.IsDue` (daily/weekly/monthly, unit-tested) + in-process `StartReportScheduler` dispatcher + CRUD + run-now; emails the report summary via an email notification channel (reusing notify + the encrypted SMTP target). Frontend: Export Center with Excel/CSV download per report, a Print/PDF button (+`@media print` chrome-hiding CSS), and a Scheduled tab (create/enable/run-now/delete). Verified live: valid 25 KB xlsx (PK zip, correct MIME), multi-sheet CSV over real devices, schedule generate returns honest "no channel — not sent" (SMTP send gated on a configured email channel). **#21 complete.**
- **#19 Work Orders Integration**: SLA + device/alert linking + history. `internal/operations.SLAMinutes`/`SLADeadline`/`ComputeSLAStatus` (pure, unit-tested) derive the deadline from priority (critical 4h / high 1d / medium 3d / low 7d) and standing (on_track/due_soon/breached/met) at read-time — no migration, consistent with license-status. Enriched `ListWorkOrdersWithDevice` (device-name join) + `ListAlertsByWorkOrder` queries; WO list/detail DTO now carries device_name + due_at + sla_status; detail returns linked_alerts; new `GET /devices/{id}/work-orders`. Frontend: SLA-breached/due-soon KPIs, SLA badge column + filters (status/priority/overdue), device link, linked-alerts table on detail, kept the timeline. Verified live (critical WO → on_track, due+4h, device name joined, device endpoint, idempotent cleanup). **#19 complete.**
- **#9 Vendor Fingerprint Library**: built `internal/fingerprint` — a pure, unit-tested matcher (OID prefix on dotted boundaries, sysDescr/HTTP/SSH substring, port) + a comprehensive 68-entry built-in library of real signatures (Cisco/Aruba/HPE/Huawei/Fortinet/Juniper/Extreme/Arista/Palo Alto/Dell/MikroTik/Ubiquiti/VMware/APC/Hikvision/Axis/Ricoh/HP/Canon/Polycom … PENs + banners + ports + low-confidence vendor-name fallbacks). API: `POST /vendor-fingerprints/seed` (idempotent import), `/match` (evidence → ranked results), `GET /devices/{id}/fingerprint-suggestion` (matches a device's stored os_version/vendor). Frontend: Import button, Match Tester, kind filter, kept CRUD. Turned dead `vendor_fingerprints` data into a working identification tool. Verified live (seed 68 idempotent; Cisco OID outranks generic SSH; real FortiGate device resolved to Fortinet/firewall). **#9 complete.** *Note:* the library powers the match tool + per-device suggestion today; auto-applying it in the live discovery classifier is deferred (documented below).
- Created progress tracker. Starting **#6 Alert Engine** (full finalization).
- **#6 Alert Engine backend** (`7b7a0ee`): migration 000029 (maintenance_windows + alert_events + alerts.acknowledged_by/escalated + alert_rules.escalate_after_minutes); engine suppression + timeline events + escalation; ack-by-actor; CRUD/timeline/note/maintenance endpoints; suppression unit test. Verified live (31 alerts opened, timeline, ack-by-alice, global window → 31 suppressed).
- **#6 Alert Engine frontend**: Alerts page rebuilt with Alerts/Rules/Maintenance tabs, escalated KPI+badge, status filter, alert timeline drawer + notes, maintenance-window scheduler/list, rule escalation field. **#6 and #20 complete.**
- **#15 Server Intelligence**: VirtualHostDetail rebuilt to the enterprise design over the real ESXi hosts — hypervisor/CPU/memory/datastore KPIs, CPU/mem/disk meters, uptime, datastore usage table (per-datastore used/total + meter), and VM inventory. Windows-specific depth (services/processes/event logs/installed software/patch) stays with the future Windows agent. **#15 complete (virtualization).**
- **#14 Firewall Intelligence**: FirewallDetail rebuilt to the enterprise design — KPI header (HA mode / active sessions / VPN up-total / cluster members), Firewall Status DefList, CPU/mem/disk meters, VPN tunnel table (up/down + in/out traffic), cluster members, licenses, interfaces. Verified live on a FortiGate (195k sessions, 10/12 tunnels up). **#14 complete.**
- **#13/#16/#17 UI finalized**: CctvDetail / WirelessDetail / PbxDetail rebuilt to the enterprise design (KPI headers, DefLists, tables, honest empty states explaining the ONVIF / wireless-REST / CUCM-AXL collection that populates the deep fields). CctvDetail verified live on a real camera (116 in fleet). The detail data itself is collection-gated (operational run), not a code gap.
- **#5b Monitoring time-series viz**: device-detail Monitoring tab rebuilt into a Performance & Availability view — uptime % (windowed), latency avg/min/max + trend sparkline, a per-sample status timeline strip, and an SNMP-metric (value_num) trend — all over the real monitoring_samples series (2,510+ samples accumulated live). **#5 complete** (tabular counters deferred + documented above).
- **#4 Topology Intelligence finalized**: `/topology/graph` with layer detection (core/distribution/access by link degree + category → edge/gateway/wireless/host), link confidence (LLDP/CDP=high, MAC=medium, ARP=low, downgraded when stale), undirected edge dedup, stale-link pruning (>7d, on every rebuild), and a layer-coloured cytoscape map with click-to-focus neighborhood. Verified live: 44 nodes / 44 deduped edges, layers core 1 / dist 23 / access 20. Engine unit test covers layer + confidence + dedup. **#4 complete.**
- **#7 Notifications** (`eb39a9e` backend + frontend): internal/notify senders (Slack/Teams/Telegram/webhook/email) + pure decision logic (severity + quiet hours, unit-tested); channels with AES-GCM-encrypted targets (never returned), background dispatcher with one-delivery-per-(channel,alert) dedup, test endpoint, delivery log; Administration → Notifications page (per-type channel form, test button, log). Verified: encrypted-at-rest blob, metadata-only DTO, test path executes + logs. **#7 complete.**
- **#8 Device Templates Engine**: turned the CRUD-only templates into an apply engine. A template now carries a structured profile (`monitoring_rules` JSONB = `{checks:[{kind tcp|snmp, port|oid, interval, down_threshold}], alerts:[{name, trigger, severity, auto_work_order}]}` — extends the existing column, no migration). `POST /device-templates/{id}/apply` (body `{device_ids}` or falls back to the template's `device_type` category) materialises real `monitoring_checks` via the idempotent `UpsertMonitoringCheck` (dedup: TCP by port, SNMP by OID) and ensures category-scoped `alert_rules` (dedup by name) — re-applying creates nothing. Frontend rebuilt: structured monitor/alert editor (no more raw JSON), a one-click starter library (Network Switch / Server / Firewall / Printer with standard ports + `sysUpTime` 1.3.6.1.2.1.1.3.0), and an Apply dialog (all-of-category or pick-devices) showing a created/skipped summary. Verified live against the real DB: apply to one switch created the SNMP check + alert rule, correctly skipped the pre-existing default TCP check, and a second apply was fully idempotent (0 created); test artifacts cleaned up. `planChecks` + defaults unit-tested. **#8 complete.**
- **#10 Config Backup + #11 Config Drift**: migration 000031 (`config_backups`: encrypted content + sha256 of normalised config + `changed` flag + version metadata); `internal/ssh` client (password auth, automatic legacy-KEX/CBC retry for old switches, host keys not pinned, password never logged); `internal/config` pure logic (per-vendor command map, CRLF/trailing-space-tolerant hash, LCS unified diff) — all unit-tested; API `POST/GET /devices/{id}/config-backups`, `GET /config-backups/{id}/content` (key-gated, the only path content leaves the server), `GET /config-backups/diff?a=&b=`, `GET /config/overview`; Operations → **Config Backup & Drift** page (eligible-device picker, version history, Back-up-now, two-version compare with colourised diff, download, fleet KPIs). Content is AES-256-GCM encrypted at rest and never in list/log responses. Verified live: overview + list + diff/content param validation; capture correctly **gates on an `ssh` credential** (fleet has none → returns `bound credential is "snmp_v2c", need an 'ssh' credential`, HTTP 400). Live config pull is operationally gated on binding an `ssh` credential (documented above). **#10 and #11 complete (live capture credential-gated).**
